1. 项目应该按照模块化的方式开发，每个模块/函数都有对应的测试代码，新增或修改代码时需要确保测试用例能够覆盖到新增或修改的代码，运行测试用例确保代码质量。
2. 当我要求review代码时，仅给出代码的review结果，不要直接对代码进行修改。
3. 增加一个功能后，除了生成自动测试代码，默认将新增的功能添加到菜单中，提供用户手动确认测试的入口。
4. 除非指定，否则图形界面中的文字默认使用中文。